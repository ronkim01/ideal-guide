# 📓 매매일지 (Trading Journal) — 기획서 v1

> 한 줄 정의: **나 혼자 쓰는, 매매 1건을 10초 안에 기록하고 승률·손익을 자동으로 계산해주는 개인용 웹 매매일지.**

---

## 1. 왜 만드나 (현재 노션 표의 문제)

현재 노션 구조: `오늘 매매 내역` / `이번 달 매매 내역` / `총 매매 내역` / `오늘 총 수익금(도넛)`
컬럼: 날짜 · 종목명 · 포지션 · 승 · 무 · 패 · 수익금 · 매매 복기

| 불편한 점 | 프로그램에서 해결 |
|---|---|
| 승·무·패를 체크박스 3개로 **수동** 선택 | 수익금만 넣으면 **자동 판정** (+면 승, 0이면 무, −면 패) |
| 오늘/이번 달/전체를 **표 3개**로 따로 관리 | 테이블 1개 + **탭 전환**만 (필터 자동) |
| 승률·손익비·기대값을 직접 계산해야 함 | **자동 집계 카드**로 항상 표시 |
| 모바일에서 표 입력이 느림 | 폰 최적화된 **한 화면 입력 폼** |
| 복기 글이 쌓이기만 하고 분석이 안 됨 | 태그 + 월간 리포트로 **패턴 도출** |

**핵심 목표 = 입력은 최소로, 계산은 전부 자동으로.**

---

## 2. 사용자와 설계 원칙

- 사용자: **1명(나)**. 공유·협업 기능 없음. 로그인 1계정.
- 원칙 3가지
  1. **입력 필드는 5개 이하** — 귀찮으면 안 쓰게 된다.
  2. **계산은 사람이 안 한다** — 승률·합계·연속패 전부 자동.
  3. **데이터는 내 것** — 언제든 CSV로 내보내고 되돌릴 수 있다.

---

## 3. 데이터 모델 — 테이블 1개면 끝

`trades` (매매 1건 = 1행, **청산 완료 기준**)

| 컬럼 | 타입 | 필수 | 설명 |
|---|---|:--:|---|
| `id` | bigint | ✅ | PK (자동) |
| `user_id` | uuid | ✅ | 내 계정 (RLS용, 자동) |
| `traded_at` | date | ✅ | 매매 날짜 (기본값 = 오늘) |
| `symbol` | text | ✅ | 코인 (예: BTC, ETH) — 자동 대문자 |
| `position` | text | ✅ | `long`(롱/매수) \| `short`(숏/매도) |
| `pnl` | numeric | ✅ | 실현 손익, USDT 기준 (손실은 음수) |
| `result` | text | ✅ | `win` \| `draw` \| `lose` — **pnl로 자동 계산, 수정 가능** |
| `memo` | text | | 매매 복기 (자유 서술) |
| `tags` | text[] | | 복기 태그 (예: `돌파`, `FOMO`, `계획대로`) |
| `created_at` | timestamptz | ✅ | 기록 시각 (자동) |

**입력 시 실제로 타이핑하는 건 4개뿐**: 코인 · 포지션 · 실현 손익 · (선택)복기
날짜는 오늘로 자동, 승패는 손익 부호에서 자동.
분할 매수·매도는 별도 컬럼 없이 **복기란에 서술**합니다 (포지션 1개 = 1줄).

### 나중에 붙일 선택 컬럼 (Phase 2, 지금은 넣지 않음)
`entry_price`(진입가) · `exit_price`(청산가) · `qty`(수량) · `strategy`(전략명) · `screenshot_url`(차트 캡처)

---

## 4. 화면 구성 — 단일 페이지 + 탭 4개

```
┌──────────────────────────────────────────────┐
│ 📓 매매일지          [오늘][이번달][전체][통계]│
├──────────────────────────────────────────────┤
│ ✍️ 매매 기록                                  │
│ [2026-09-21] [BTC____] [롱|숏] [손익 USDT__]  │
│ [복기____________________]      [ 기록하기 ]  │
├──────────────────────────────────────────────┤
│            오늘 실현 손익                      │
│              +$465.32                         │
│           3건 · 2승 0무 1패                    │
├──────────────────────────────────────────────┤
│ 승률 66.7%  │ 최고 +$291.90 │ 최대손실 −$90.00│
│ ●●●●●●──── │               │                 │
├──────────────────────────────────────────────┤
│ 09.21  SOL  숏  ●승            +$291.90       │
│ 09.21  ETH  롱  ●승            +$263.42       │
│ 09.21  BTC  숏  ●패             −$90.00       │
│        └ 4h 저항 돌파 후 3회 분할 진입...      │
└──────────────────────────────────────────────┘
```

| 탭 | 보여주는 것 |
|---|---|
| **오늘** | 빠른 입력 폼 + 오늘 손익/승률 카드 + 오늘 목록 |
| **이번 달** | 월 누적 손익 + 일별 손익 막대그래프 + 월 목록 |
| **전체** | 기간·코인·포지션·결과 필터 + 누적 손익 곡선 + 전체 목록 |
| **통계** | 아래 8개 지표 + 코인별·요일별 성적 |

---

## 5. 자동 계산 지표 (직접 계산할 필요 없는 것들)

| 지표 | 계산식 |
|---|---|
| 총 손익 | Σ pnl |
| 승률 | 승 ÷ (승 + 패) |
| 평균 수익 | 이익 거래 pnl 평균 |
| 평균 손실 | 손실 거래 pnl 평균(절댓값) |
| **손익비** | 평균 수익 ÷ 평균 손실 |
| **기대값** | (승률 × 평균수익) − (패율 × 평균손실) — *1회 매매당 기대 금액* |
| 최대 연속 패 | 연속 `lose` 최대 길이 |
| 최대 낙폭(MDD) | 누적 손익 곡선의 고점 대비 최대 하락폭 |

> 손익비와 기대값이 이 프로그램의 진짜 가치입니다. 승률 70%여도 기대값이 음수면 계좌는 녹습니다.

---

## 6. 기능 목록 & 개발 단계

| 단계 | 기능 | 예상 |
|---|---|---|
| **P0 — 동작하는 최소본** | 로그인 1계정, 매매 기록 추가/수정/삭제, 오늘 목록, 오늘 손익·승률 카드 | 반나절 |
| **P1 — 쓸 만해짐** | 이번 달/전체 탭, 통계 8개 지표, 누적 손익 차트, 모바일 반응형 | 하루 |
| **P2 — 편해짐** | 기간·종목·결과 필터, 검색, 복기 태그, CSV 내보내기/가져오기, 다크모드 | 하루 |
| **P3 — 똑똑해짐** | 월간 AI 복기 리포트 (복기 글 + 통계를 모아 반복 실수 패턴 요약) | 반나절 |

**P3는 이 저장소의 기존 자산을 그대로 씁니다.** `@anthropic-ai/sdk`가 이미 설치돼 있고 GitHub Actions 수동 실행 버튼도 있으니, 월말에 버튼 한 번 → `workspace/`에 월간 매매 리포트 생성 구조를 그대로 재사용할 수 있습니다.

---

## 7. 기술 스택 — 추천안

**추천: 지금 저장소 스택 그대로 (정적 HTML/CSS/Vanilla JS + Supabase + GitHub Pages)**

이유 3가지:
1. `index.html` + `app.js` + `styles.css` + `supabase-setup.sql` 구조가 이미 돌아가고 있음 → **새로 배울 것 0**
2. 폰·PC 어디서든 같은 데이터 (Supabase Postgres에 저장)
3. **비용 0원** (Supabase 무료 티어 + GitHub Pages 무료)

| 대안 | 장점 | 단점 | 판단 |
|---|---|---|---|
| 완전 로컬 (localStorage만) | 서버·로그인 불필요, 가장 단순 | 폰↔PC 동기화 안 됨, 브라우저 정리하면 **데이터 날아감** | ❌ 기록용으론 위험 |
| Next.js + Vercel | 확장성 좋음 | 1인용 기록 앱엔 과함, 빌드 학습 필요 | ❌ 과설계 |
| **정적 + Supabase** | 위 3가지 | 초기 로그인 세팅 10분 | ✅ **채택** |

---

## 8. 보안 — 방명록 정책을 그대로 쓰면 안 됩니다 ⚠️

현재 `supabase-setup.sql`은 **"누구나 읽기 / 누구나 쓰기"** 정책입니다. 방명록은 그게 맞지만, 매매일지는 내 손익이 그대로 노출되고 누구나 지울 수 있게 됩니다.

→ **Supabase Auth 이메일 로그인 1계정 + RLS `user_id = auth.uid()`** 로 잠급니다. (내 행만 읽기·쓰기·삭제 가능)

### SQL 스키마 초안

```sql
create table if not exists public.trades (
  id         bigint generated always as identity primary key,
  user_id    uuid not null default auth.uid() references auth.users(id) on delete cascade,
  traded_at  date not null default current_date,
  symbol     text not null,
  position   text not null check (position in ('long','short')),
  pnl        numeric(14,2) not null default 0,
  result     text not null check (result in ('win','draw','lose')),
  memo       text,
  tags       text[] not null default '{}',
  created_at timestamptz not null default now()
);

create index if not exists trades_user_date_idx on public.trades (user_id, traded_at desc);

alter table public.trades enable row level security;

create policy "내 기록만 조회" on public.trades
  for select to authenticated using (auth.uid() = user_id);
create policy "내 기록만 추가" on public.trades
  for insert to authenticated with check (auth.uid() = user_id);
create policy "내 기록만 수정" on public.trades
  for update to authenticated using (auth.uid() = user_id);
create policy "내 기록만 삭제" on public.trades
  for delete to authenticated using (auth.uid() = user_id);
```

### 백업
주 1회 CSV 내보내기(P2) 또는 Supabase 대시보드에서 테이블 다운로드. 무료 티어는 **일정 기간 접속이 없으면 프로젝트가 일시 중지**될 수 있으므로 CSV 백업을 습관화합니다.

---

## 9. 확정 사항 (2026-09-21)

1. **자산군·통화 → 코인 / USDT 기준.** 화면 우측 상단에서 `USDT ↔ 원화` 표시 전환 가능(표기만 바뀌며 환산은 하지 않음).
2. **폰·PC 모두 사용 → 웹 + Supabase 저장.** 로컬 저장 없이 어디서 접속해도 같은 데이터. GitHub Pages로 배포해 폰 홈 화면에 추가하면 앱처럼 사용.
3. **기록 단위 → 포지션 1개 = 1줄.** 분할 매수·매도는 별도 컬럼 대신 **복기란에 서술**. (진입가·수량 컬럼은 넣지 않음)

---

## 10. 구현 완료 (P0 ~ P2)

`journal/` 폴더에 구현되어 있습니다. 설치·사용법은 [`../journal/README.md`](../journal/README.md).

| 단계 | 범위 | 상태 |
|---|---|:--:|
| P0 | 로그인, 기록 추가/수정/삭제, 오늘 목록, 오늘 손익·승률 | ✅ |
| P1 | 이번 달/전체/통계 탭, 지표 8종, 일별 손익 막대, 누적 손익 곡선, 모바일 대응 | ✅ |
| P2 | 기간·코인·포지션·결과 필터, 복기 태그, CSV 내보내기, 다크모드 | ✅ |
| P3 | 월간 AI 복기 리포트 (복기 글 + 통계로 반복 실수 패턴 요약) | 미착수 |

```
journal/
  index.html          화면 구조
  app.js              집계 · 차트 · CRUD · CSV
  styles.css          라이트/다크 자동 전환
  supabase-setup.sql  trades 테이블 + RLS 보안 정책
  README.md           설치 · 사용법 · 지표 해설
```

### 설계상 노션과 달라진 점

| 노션 | 구현 |
|---|---|
| 표 3개(오늘/이번달/전체)를 따로 관리 | 테이블 1개 + 탭 전환 (필터 자동) |
| 승·무·패 체크박스 3개 수동 선택 | 손익 부호로 자동 판정 (수동 지정도 가능) |
| `오늘 총 수익금` 도넛 차트 | **큰 숫자 한 줄로 교체** — 2조각 도넛은 숫자 하나보다 읽기 어렵고 자리만 차지함 |
| 승률·합계 수동 계산 | 승률·손익비·기대값·최대낙폭·최대연속패 자동 |

### 색 규칙

수익 = 빨강(`#e34948`), 손실 = 파랑(`#2a78d6`) — 국내 차트 관례를 따랐습니다.
버튼·강조는 무채색(먹색)으로 두어 **색은 데이터에만** 씁니다.
색각 이상(적록·청황) 시뮬레이션과 배경 대비를 검증했고(라이트/다크 모두 통과),
색에만 의존하지 않도록 결과는 `승`/`무`/`패` 글자로도 함께 표시합니다.

---

## 11. 다음 단계 후보

- **P3 월간 AI 복기 리포트** — 이 저장소에 이미 `@anthropic-ai/sdk`와 GitHub Actions 수동 실행 버튼이 있으므로, 월말에 버튼 한 번으로 `workspace/`에 리포트를 생성하는 구조를 그대로 재사용할 수 있습니다.
- **CSV 가져오기** — 기존 노션 표를 옮겨올 때 필요.
- **진입가·수량·레버리지 컬럼** — 분할 매매를 숫자로 집계하고 싶어지면 그때 추가.
